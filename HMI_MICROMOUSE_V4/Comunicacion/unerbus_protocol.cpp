#include "unerbus_protocol.h"

// Este archivo se crea para asegurar que el Meta-Object Compiler (moc) de Qt
// procese correctamente el archivo de cabecera 'unerbus_protocol.h'.
// Al contener Q_NAMESPACE y Q_ENUM_NS, el header necesita este procesamiento
// para generar el código necesario (Unerbus::staticMetaObject) y evitar
// errores del enlazador (linker errors).